<h1>Your cart</h1>

<ul>
  <% cart.each do |item|%>
    <li><%= item %></li>
  <% end %>
</ul>

<h1>Add Items to Your Cart</h1>

  <%= form_tag('/add') do %>
    <%= text_field_tag('product') %>
    <%= submit_tag "add to cart" %>
  <% end %>
