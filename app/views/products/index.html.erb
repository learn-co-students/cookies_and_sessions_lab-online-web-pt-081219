<h1>Products#index</h1>

<ul>
    <% cart.each do |product| %>
        <li><%= product %></li>
    <% end %>
</ul>

<h1>Add a New Product</h1>

<%= form_tag root_path do %>
<%= label_tag "Insert Product Into Cart"%>
  <%= text_field_tag :product %>
  <%= submit_tag "add to cart" %>
<% end %>